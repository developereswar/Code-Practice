// Block Swap 

var arr = ['e', 's', 'w', 'a', 'r'];
var n = arr.length;

function blockSwap(arr, d, n){
	if(n < d) return "invalid data";

	let result = [];

	result = arr.slice(d);

	for(let i =0; i<d; i++){
		result.push(arr[i])
	}
	return result;
}

